apiVersion: v1
kind: ConfigMap
metadata:
  name: yaml-configmap
  namespace: default
data:
  settings.yaml: |
    app:
      mode: debug
      timeout: 30s
    features:
      logging: enabled
      monitoring: disabled